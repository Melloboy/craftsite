{% extends "_layout" %}

{% block content %}

{% macro errorList(errors) %}
    {% if errors %}
        <ul class="errors">
            {% for error in errors %}
                <li>{{ error }}</li>
            {% endfor %}
        </ul>
    {% endif %}
{% endmacro %}

<form method="post" accept-charset="UTF-8">
    {{ csrfInput() }}
    {{ actionInput('entries/save-entry') }}
    {{ redirectInput('about') }}
    {{ hiddenInput('sectionId', '4') }}
    {{ hiddenInput('enabled', '1') }}
    {{entry.sectionId}}
    <label for="title">Title</label>
    <input id="title" type="text" name="title"
        {%- if entry is defined %} value="{{ entry.title }}"{% endif -%}>

    {% if entry is defined %}
        {{ _self.errorList(entry.getErrors('title')) }}
    {% endif %}

    <label for="body">Body</label>
    <textarea id="body" name="fields[body]">
        {%- if entry is defined %}{{ entry.body }}{% endif -%}
    </textarea>

    {% if entry is defined %}
        {{ _self.errorList(entry.getErrors('body')) }}
    {% endif %}

    <input type="submit" value="Publish">
</form>

<div class="img-container" style='background-image: url("https://images.pexels.com/photos/2944542/pexels-photo-2944542.jpeg") !important;'>
  <!-- <img style="width: 100%; opacity: 65%" src="https://images.pexels.com/photos/2944542/pexels-photo-2944542.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=750&w=1260"> -->
  
  <div class="overlay">
   
        <div class="col-md-6 col-sm-12 purple-bg"> 
    <span> 
        <div>
            <h1 class="type-callout">Get hired.</h1>
            <h1 class="type-callout">Gain skills.</h1>
            <h1 class="type-callout">Go places.</h1>
            <p> Cutting-edge technology, endless variety, growth opportunities. All in a day’s work as a skateboarder. </p>
           <button type="button" class="btn btn-danger btn-sm">Kickflip</button>
        </div>
    </span>
    </div>
  
</div>
</div>


<!-- 
<h1 class="text-4xl text-black font-display my-4">{{ entry.title }}</h1>

<div class="my-8">
		  {% for block in entry.postContent.all() %}
		    <div class="my-4">
		      {% if block.type == 'text' %}
		        <div class="copy">
		          {{ block.text }}
		        </div>
		      {% elseif block.type == 'image' %}
		        {% for image in block.image.all() %}
		          <img style="width: 50%; margin-bottom: 2%" class="my-8" src="{{ image.url }}" alt="{{ image.title }}" />
		        {% endfor %}
		      {% endif %}
		    </div>
		  {% endfor %}
		</div> 

		<div class="container">
			<div class="row">
				<div class="col-sm-6">
					<div id="carouselExampleSlidesOnly" class="firstcarousel carousel slide" data-ride="carousel">
						<div class="carousel-inner">
							{% if entry.aboutImage | length %}
								{% for key, image in entry.aboutImage.all() %}
								<div class="{{ key == 0 ? 'carousel-item active' : 'carousel-item'}}">
											<img src="{{ image.getUrl({ width: 300, height: 300 }) }}" alt="{{ image.title }}" class="my-8 rounded-0"/>
										</div>
								{% endfor %}
							{% endif %}
							<a class="carousel-control-prev" href="#carouselExampleSlidesOnly" role="button" data-slide="prev">
								<span class="carousel-control-prev-icon" aria-hidden="true"></span>
								<span class="sr-only">Previous</span>
							</a>
							<a class="carousel-control-next" href="#carouselExampleSlidesOnly" role="button" data-slide="next">
								<span class="carousel-control-next-icon" aria-hidden="true"></span>
								<span class="sr-only">Next</span>
							</a>
						</div>
					</div>

				</div>
			</div>
		</div>

		<!-- <div class="flex -mx-4">
		    <div class="w-1/2 px-4">
		      {% if entry.aboutImage | length %}
		        {% for image in entry.aboutImage.all() %}
		          <img src="{{ image.getUrl({ width: 300, height: 300 }) }}" alt="{{ image.title }}" class="my-8 rounded-0" />
		        {% endfor %}
		      {% endif %}
		
		      -->

<!-- Carousel Entry 
		
		<div id="carouselExampleSlidesOnly" class="carousel slide skating" data-ride="carousel">
		  <div class="carousel-inner">
		    {% if entry.carousel | length %}
		        {% for key, image in entry.carousel.all() %}
		        {% if key == 0 %}
		          <div class="carousel-item active">
		            <img src="{{ image.getUrl({ width: 300, height: 300 }) }}" alt="{{ image.title }}" class="my-8 rounded-0" />
		        </div>
		{% else %}
		<div class="carousel-item">
		            <img src="{{ image.getUrl({ width: 300, height: 300 }) }}" alt="{{ image.title }}" class="my-8 rounded-0" />
		        </div>
		{% endif %}
		
		
		        {% endfor %}
		      {% endif %}
		  </div>
		</div> -->{% endblock %}
